# .env.example
# Not needed when running inside Claude Artifacts (auth is handled automatically).
# Only required if you adapt this for standalone use outside Claude.

ANTHROPIC_API_KEY=sk-ant-your-key-here
